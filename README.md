# EVERYONE, ```git pull``` before doing a pull request!

## Run this locally to load http resources in Unity WebGL(for now)
```
  cd into/this/directory
  npm install -g http-server
  http-server
```
